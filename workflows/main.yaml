name: 'test my project'
on: [push] # список событий на которые запускается действие
jobs: # список работ которые будут делаться (каждая работа будет
  checks: # имя работы сhecks
  runs-on: ubuntu-latest # на какой машине делать работы (можно задать
  steps: # выполняемые последовательно шаги
  - uses: actions/checkout@v3
  - run: echo "hello world"